const network = {
    success: {
      inside: 200
    },
  
    fail : {
        "alert01" : "message global test"
    },
  
    error : {
  
    }
  };
  
  const success = {
      restApl: {
        inside: 200
      },
  };
  
  
  const fail = {
      restApl: {
  
      },
  };
  
  
  const error = {
      restApl: {
  
      },
  };
  
  export {success, fail, error, network};
  